# NightGuard 夜间探索型工具收束限制｜Codex 执行 Prompt

## 一、任务结论

本次不是新增复杂功能，而是修复 NightGuard 当前规则缺口：

晚上 23:00 后，对 Codex / GPT / Cursor / AI 网站等探索型工具增加“收束限制”，避免夜间技术探索、自我分析、AI 对话导致熬夜。

核心原则：

- 白天不影响正常工作；
- 23:00 - 23:30 只提醒，不拦截；
- 23:30 - 次日 07:00 进入收束提醒模式；
- 可选择“本晚不再限制”“记录到明天并最小化”“稍后再提醒我”；
- “稍后再提醒我”会在 15 分钟后重新提醒；
- 不影响必要工作工具和系统进程；
- hosts 写入失败必须记录并提示；
- dotnet build 和 dotnet publish 必须通过。

---

## 二、背景

用户晚上容易因为 Codex、ChatGPT、Cursor、技术探索、自我分析继续兴奋，导致 0 点后还停不下来。

但这些工具白天和必要工作时也有用，所以不能永久禁用。

本任务目标不是强制锁死，而是做“夜间收束”：

- 23:00 后降低新问题开启概率；
- 23:30 后明确提示收束选择；
- 把“继续探索”的冲动转为“记录到明天再做”。

---

## 三、夜间探索型工具名单

新增或扩展一个“夜间探索型工具名单”。

默认包含：

### 进程名

- Codex.exe
- Cursor.exe

### 网站域名

- chatgpt.com
- chat.openai.com
- claude.ai
- gemini.google.com
- perplexity.ai

要求：

1. 不要影响普通浏览器整体使用。
2. 网站限制只针对指定 AI 探索网站。
3. 进程名匹配要大小写不敏感。
4. 后续名单应方便扩展。
5. 如果项目已有规则配置结构，优先复用现有配置结构。
6. 如果新增配置字段，需要兼容旧配置，旧配置缺字段时使用默认值。

---

## 四、时间段规则

### 1. 白天正常模式

时间范围：

- 07:00 - 23:00

行为：

- 不限制 Codex / GPT / Cursor / AI 网站；
- 不弹窗；
- 不写 hosts 限制；
- 不杀进程；
- 不影响正常工作。

---

### 2. 收尾提醒模式

时间范围：

- 23:00 - 23:30

行为：

- 不直接拦截；
- 第一次触发某个探索型工具时弹窗提醒；
- 同一晚同一工具只提醒一次；
- 不影响继续使用。

弹窗文案：

> 现在已进入夜间收尾时间，建议不要开启新问题，只做保存、整理、记录明天任务。

要求：

1. 同一晚同一工具只提醒一次。
2. 不要每次轮询都弹窗。
3. 不写 hosts 限制。
4. 不杀进程。
5. 记录一条提醒日志，方便首页最近记录查看。

---

### 3. 收束提醒模式

时间范围：

- 23:30 - 次日 07:00

行为：

打开探索型工具时弹窗，选项只保留：

- A. 本晚不再限制
- B. 记录到明天并最小化
- C. 稍后再提醒我

要求：

1. “本晚不再限制”表示本晚剩余夜间探索时段不再提醒该应用。
2. 如果选择“记录到明天并最小化”，应记录日志或写入已有记录区域，并尝试最小化对应应用主窗口，不需要做复杂待办系统。
3. 如果选择“稍后再提醒我”，暂缓 15 分钟后重新提醒。
4. 弹窗关闭也按“稍后再提醒我”处理。
5. 不要误杀系统进程。
6. 不要影响非探索型工具。
7. 如果已有弹窗/临时放行机制，优先复用，但本场景不再使用限时收尾次数。

---

## 五、网站 hosts 限制要求

针对 AI 探索网站：

- chatgpt.com
- chat.openai.com
- claude.ai
- gemini.google.com
- perplexity.ai

只在以下时间生效：

- 23:30 - 次日 07:00

要求：

1. 23:00 - 23:30 不写 hosts，只提醒。
2. 07:00 - 23:00 必须恢复 hosts。
3. 退出限制后必须恢复 hosts。
4. 如果 hosts 写入失败：
   - 写入日志；
   - 在首页或已有提示区域显示；
   - 程序不能崩溃。
5. 不要影响其它普通网站。
6. 不要改动与本任务无关的 hosts 规则。

---

## 六、必要工作保护

本任务不得影响以下内容：

### 不限制

- 微信
- 文档工具
- IDE
- 终端
- 浏览器普通网页
- ActivityWatch
- Clash
- PixPin

### 不误杀

- Windows 系统进程
- Explorer
- Shell
- 驱动或安全软件
- dotnet
- powershell
- cmd
- Windows Terminal

要求：

1. 进程限制只针对夜间探索型工具名单。
2. 浏览器进程本身不要直接禁用。
3. 网站限制通过指定域名处理，不要封整个浏览器。
4. 如果无法识别浏览器当前网页，不要误杀浏览器。

---

## 七、日志与首页提示

如果项目已有“最近拦截记录面板”，本任务应写入记录：

记录类型建议：

- 夜间收尾提醒
- 夜间探索收束弹窗
- 本晚不再限制
- 记录到明天
- 稍后再提醒
- hosts 写入失败
- hosts 恢复成功
- hosts 恢复失败

每条记录至少包含：

- 时间
- 类型
- 目标
- 动作
- 结果
- 备注

如果首页已有状态提示区域，hosts 写入失败需要显示。

---

## 八、状态计数规则

需要维护夜间状态：

1. 同一晚同一工具 23:00 - 23:30 只提醒一次。
2. 23:30 - 次日 07:00 选择“本晚不再限制”后，本晚剩余时间不再提醒该应用。
3. 选择“稍后再提醒我”或关闭弹窗后，15 分钟后重新提醒。
4. “一晚”的定义建议：
   - 当天 23:00 到次日 07:00 属于同一个 nightKey；
   - 例如 2026-05-28 23:00 到 2026-05-29 07:00，都算 2026-05-28 这一晚。
5. 到第二晚自动重置提醒记录和本晚不再限制状态。
6. 程序重启后，如果已有配置/状态持久化机制，可持久化；如果没有，不强制新增复杂持久化，但代码结构要方便后续扩展。

---

## 九、构建与发布要求

完成后必须验证：

```powershell
dotnet build
```
