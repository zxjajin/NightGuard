# NightGuard Real Test Checklist

> 本清单只用于真实验收测试，不代表自动化测试已通过。涉及 hosts、进程拦截和管理员权限，请在可恢复的环境中执行。

## 测试前准备

- [ ] 确认已生成发布版：`publish/NightGuard/NightGuard.exe`
- [ ] 关闭正在运行的旧版 NightGuard，避免多个实例同时处理 hosts 或进程。
- [ ] 记录当前配置，必要时先导出配置 JSON。
- [ ] 准备恢复方式：`Emergency-Restore.bat` 或 `Emergency-Restore.ps1`
- [ ] 确认当前 hosts 文件没有其他临时测试改动。

## 1. 管理员身份运行发布版

- [ ] 右键 `publish/NightGuard/NightGuard.exe`，选择“以管理员身份运行”。
- [ ] 首页“启动自检”中确认：
  - [ ] 管理员权限：是
  - [ ] hosts 可写：是
  - [ ] 当前限制中：按实际状态显示
- 判定：
  - [ ] 通过：程序正常启动，自检状态清晰。
  - [ ] 不通过：程序无法启动、自检异常、hosts 可写状态与实际不符。

## 2. 临时把限制时间改成当前时间附近

- [ ] 进入设置页并解锁设置。
- [ ] 将限制开始时间改为当前时间前 1-2 分钟。
- [ ] 将限制结束时间改为当前时间后 5-10 分钟。
- [ ] 保存设置。
- [ ] 回到首页，观察是否进入限制状态。
- 判定：
  - [ ] 通过：进入限制模式，并在“最近拦截记录”出现限制开始记录。
  - [ ] 不通过：未进入限制模式，或状态/记录不一致。

## 3. 测试 Steam / WeGame 是否被拦

- [ ] 确认规则中包含 `steam.exe`、`steamwebhelper.exe`、`wegame.exe` 或相关 WeGame 进程。
- [ ] 在限制期间启动 Steam。
- [ ] 在限制期间启动 WeGame。
- [ ] 查看进程是否被结束。
- [ ] 查看“最近拦截记录”是否记录进程名、动作和结果。
- 判定：
  - [ ] 通过：Steam / WeGame 被拦截，记录中能看到对应进程。
  - [ ] 不通过：规则已包含但未被拦截。
  - [ ] 规则缺口：当前规则没有覆盖实际启动的 Steam / WeGame 进程名。

## 4. 测试非白名单前台应用是否弹窗

- [ ] 确认“限制期间禁用非白名单前台应用”已开启。
- [ ] 选择一个不在白名单中的普通前台应用。
- [ ] 在限制期间启动该应用。
- [ ] 观察是否弹出单独授权窗口。
- 判定：
  - [ ] 通过：弹出授权窗口，可选择允许 1 分钟、今晚不限等选项。
  - [ ] 不通过：未弹窗且应用被静默放行，或弹窗选项不可用。
  - [ ] 规则缺口：该应用实际在白名单或内置允许列表中，因此不能证明前台弹窗覆盖有效。

## 5. 测试允许 1 分钟后是否到期重新限制

- [ ] 在授权窗口中选择“允许 1 分钟”。
- [ ] 保持该应用运行。
- [ ] 等待超过 1 分钟。
- [ ] 再次打开或激活该应用。
- 判定：
  - [ ] 通过：1 分钟后授权到期，应用重新受到限制或再次触发处理。
  - [ ] 不通过：超过 1 分钟后仍持续放行。

## 6. 测试今晚不限是否只到本轮结束

- [ ] 在限制期间对某个非白名单应用选择“今晚不限”。
- [ ] 确认本轮限制时间内该应用被放行。
- [ ] 将限制结束时间调到当前时间附近，等待本轮限制结束。
- [ ] 再将限制时间改到新的当前时间附近，进入新一轮限制。
- [ ] 再次启动同一应用。
- 判定：
  - [ ] 通过：今晚不限只在本轮限制结束前有效，新一轮限制中重新受控。
  - [ ] 不通过：新一轮限制中仍被永久放行。

## 7. 测试 hosts 是否真的屏蔽 douyin.com

- [ ] 确认“启用网站 hosts 限制”已开启。
- [ ] 确认域名规则包含 `douyin.com`、`www.douyin.com`。
- [ ] 进入限制模式。
- [ ] 检查 hosts 文件中存在 NightGuard 区块和 douyin 相关域名。
- [ ] 在浏览器访问 `https://www.douyin.com`。
- [ ] 可用命令辅助检查：`ping www.douyin.com`
- 判定：
  - [ ] 通过：hosts 中写入 douyin 规则，浏览器访问失败或解析到本机。
  - [ ] 不通过：hosts 未写入、写入失败、或 douyin 仍正常访问。
  - [ ] 风险记录：浏览器 DNS 缓存、DoH、代理/TUN 可能绕过 hosts，需要单独说明。

## 8. 测试 ChatGPT / YouTube 是否按当前配置不拦

- [ ] 检查当前 hosts 黑名单和进程黑名单。
- [ ] 确认没有配置 `chatgpt.com`、`openai.com`、`youtube.com`、`googlevideo.com` 等域名。
- [ ] 确认浏览器进程在白名单或内置允许列表中。
- [ ] 在限制期间访问 ChatGPT。
- [ ] 在限制期间访问 YouTube。
- 判定：
  - [ ] 通过：在当前配置未覆盖这些站点时，ChatGPT / YouTube 不被 NightGuard 拦截。
  - [ ] 不通过：当前配置未覆盖但仍被拦截，需要定位是否被进程规则、hosts、代理或其他软件影响。
  - [ ] 规则已覆盖：如果当前配置实际包含相关域名或进程，则不能标记“通过不拦”，应按配置预期重新判断。

## 9. 测试 Codex / GPT / 浏览器探索型工具是否被当前规则覆盖

- [ ] 列出实际使用的工具和进程名，例如：
  - [ ] Codex
  - [ ] GPT 客户端或浏览器标签页
  - [ ] 浏览器探索型工具
  - [ ] Node / extension host / helper process
- [ ] 检查这些进程是否在白名单、内置允许列表、进程黑名单中。
- [ ] 检查相关域名是否在 hosts 黑名单中，例如 OpenAI、ChatGPT、搜索、浏览器自动化依赖域名。
- [ ] 在限制期间分别启动或使用这些工具。
- [ ] 观察是否被拦截、弹窗、或完全放行。
- 判定：
  - [ ] 通过：当前规则明确覆盖了目标工具，并且实际行为符合预期。
  - [ ] 不通过：当前规则明确覆盖，但实际没有拦截或误拦。
  - [ ] 规则缺口：当前规则没有覆盖 Codex / GPT / 浏览器探索型工具的实际进程名或域名。

## 10. 规则缺口记录要求

- [ ] 如果 Steam / WeGame、ChatGPT / YouTube、Codex / GPT / 浏览器探索型工具没有被当前规则覆盖，不要写“测试通过”。
- [ ] 按下面格式记录：

```text
规则缺口：
- 对象：
- 实际进程名：
- 实际域名：
- 当前配置是否覆盖：否
- 观察到的行为：
- 建议补充规则：
```

## 测试后恢复

- [ ] 将限制时间恢复为日常使用配置。
- [ ] 如测试中启用了 hosts 限制，点击“立即恢复 hosts”。
- [ ] 如 hosts 状态异常，运行 `Emergency-Restore.bat` 或 `Emergency-Restore.ps1`。
- [ ] 导出或保存最终有效配置。
- [ ] 退出 NightGuard 后重新启动一次，确认配置仍保留。
