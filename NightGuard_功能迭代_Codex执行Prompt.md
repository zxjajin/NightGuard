# NightGuard 功能迭代：Codex 分步执行 Prompt

## 使用方式

把下面内容按阶段复制给 Codex，不要一次性全部执行。  
每完成一个阶段，都先运行、检查、提交，再进入下一阶段。

目标不是重构整个项目，而是在现有 NightGuard 基础上稳定补充功能。

---

# 总体要求

你现在要在现有 NightGuard 项目上做功能迭代。请严格遵守以下原则：

1. 不要推翻现有架构，不要大重构。
2. 优先小步修改，每一步都能编译运行。
3. 每完成一个阶段后，说明：
   - 修改了哪些文件
   - 新增了哪些类/方法
   - 如何手动测试
   - 是否存在风险
4. 涉及 hosts、管理员权限、自启动、进程拦截的逻辑，必须保持谨慎，不能扩大影响范围。
5. 所有新增功能都要尽量有日志记录。
6. UI 文案使用中文，保持简洁。
7. 如果现有项目没有对应模块，请先查找最接近的模块再补充，不要凭空新建一套平行系统。

---

# 阶段 1：最近拦截记录面板

## 目标

在首页新增“最近拦截记录”面板，显示最近 20 条 NightGuard 执行过的拦截相关操作。

这个功能用于快速判断：出问题时，NightGuard 到底动了哪个应用、哪个 hosts、哪个规则。

## 需要记录的事件

至少记录以下类型：

1. 应用进程被拦截
2. hosts 规则被写入
3. hosts 规则被移除
4. 限制模式开始
5. 限制模式结束
6. 测试模式触发的拦截事件
7. 执行失败的拦截操作，例如 hosts 写入失败、权限不足、进程关闭失败

## 数据字段

每条记录至少包含：

```text
时间
类型
目标
动作
结果
备注
```

示例：

```text
2026-05-28 23:10:12 | 应用拦截 | steam.exe | 结束进程 | 成功 | 娱乐规则命中
2026-05-28 23:10:15 | hosts 写入 | douyin.com | 添加规则 | 成功 | 抖音模板
2026-05-28 23:11:02 | hosts 写入 | bilibili.com | 添加规则 | 失败 | hosts 文件不可写
```

## 实现要求

1. 新增一个拦截记录模型，例如 `BlockRecord` 或 `GuardActionRecord`。
2. 新增一个记录服务，例如 `BlockRecordService`。
3. 最近记录保存在内存中即可，最多保留 100 条。
4. 首页只显示最近 20 条。
5. 记录顺序：最新的在最上面。
6. 如果项目已有日志或状态服务，优先复用，不要重复造轮子。
7. UI 面板不要太复杂，表格或列表都可以。
8. 如果是 WPF 项目，优先使用 ObservableCollection 绑定。
9. 如果是 WinForms 项目，优先使用 DataGridView 或 ListView。

## Codex 执行 Prompt

请在现有 NightGuard 项目中新增“最近拦截记录”功能。

要求：

- 在首页显示最近 20 条拦截/hosts/限制状态变化记录。
- 新增记录模型，字段包括：时间、类型、目标、动作、结果、备注。
- 新增记录服务，内存最多保留 100 条，首页展示最新 20 条。
- 在现有进程拦截、hosts 修改、限制开始/结束、测试模式相关代码中插入记录调用。
- 不要大重构现有逻辑。
- 如果某些拦截入口暂时不好接入，请先接入最核心的进程拦截和 hosts 修改入口，并明确说明遗漏点。
- 修改完成后给出修改文件清单、测试步骤、风险点。

## 验收标准

1. 启动软件后首页能看到“最近拦截记录”。
2. 进入限制模式后，面板出现限制开始记录。
3. 退出限制模式后，面板出现限制结束记录。
4. 拦截应用时，能看到被拦截进程名。
5. 修改 hosts 时，能看到 hosts 目标和结果。
6. 记录最多显示 20 条，最新在上。
7. 程序重启后记录可以清空，暂时不要求持久化。

---

# 阶段 2：娱乐规则模板

## 目标

新增“一键添加娱乐规则模板”，减少手动维护域名和进程名的成本。

## 需要支持的模板

至少包含：

```text
Steam
WeGame
抖音
B站
快手
小红书
虎牙
斗鱼
```

## 每个模板包含内容

每个模板建议包含：

```text
模板名称
应用进程名列表
hosts 域名列表
备注
```

示例：

```text
Steam
进程：steam.exe, steamwebhelper.exe
域名：store.steampowered.com, steamcommunity.com, steampowered.com
备注：Steam 商店和社区访问限制
```

## 注意

域名和进程名不要求一次性百分百完整，但要采用可维护结构，方便后续补充。

## 实现要求

1. 新增规则模板模型，例如 `RuleTemplate`。
2. 新增模板服务，例如 `RuleTemplateService`。
3. 在设置页或规则页新增“娱乐规则模板”区域。
4. 用户可以选择某个模板并一键添加。
5. 添加前要做去重，避免重复添加同一个进程或域名。
6. 添加后要提示添加结果，例如：
   - 新增进程规则 2 条
   - 新增 hosts 规则 3 条
   - 已跳过重复规则 1 条
7. 添加模板时，要写入最近拦截记录或普通操作日志。

## 推荐初始模板

```text
Steam
进程：steam.exe, steamwebhelper.exe
域名：store.steampowered.com, steamcommunity.com, steampowered.com

WeGame
进程：wegame.exe, tgp_daemon.exe
域名：wegame.com.cn, tgp.qq.com

抖音
进程：douyin.exe
域名：douyin.com, www.douyin.com, live.douyin.com, snssdk.com, amemv.com

B站
进程：bilibili.exe
域名：bilibili.com, www.bilibili.com, live.bilibili.com, api.bilibili.com

快手
进程：kuaishou.exe
域名：kuaishou.com, www.kuaishou.com, gifshow.com

小红书
进程：xiaohongshu.exe
域名：xiaohongshu.com, www.xiaohongshu.com, xhscdn.com

虎牙
进程：huya.exe
域名：huya.com, www.huya.com, live.huya.com

斗鱼
进程：douyu.exe
域名：douyu.com, www.douyu.com, live.douyu.com
```

## Codex 执行 Prompt

请在现有 NightGuard 项目中新增“娱乐规则模板”功能。

要求：

- 新增模板模型和模板服务。
- 模板至少包括 Steam、WeGame、抖音、B站、快手、小红书、虎牙、斗鱼。
- 每个模板包含进程名列表和 hosts 域名列表。
- 在现有规则管理页面或设置页面增加“一键添加模板”入口。
- 添加模板时自动去重，不要重复添加已有进程或域名。
- 添加后显示新增数量和跳过重复数量。
- 添加行为要写入日志或最近操作记录。
- 不要破坏现有手动添加规则功能。
- 完成后说明修改文件、测试步骤、风险点。

## 验收标准

1. 页面上能看到娱乐规则模板入口。
2. 点击 Steam 模板后，能自动添加 steam.exe 等进程和 Steam 相关域名。
3. 重复点击同一模板，不会产生重复规则。
4. 添加结果有提示。
5. 原有手动添加规则功能仍正常。

---

# 阶段 3：设置导入/导出

## 目标

新增配置导入/导出，防止配置丢失，也方便换电脑。

## 导出内容

至少导出：

1. 应用拦截规则
2. hosts 拦截规则
3. 时间限制配置
4. 自启动配置状态，如果项目中已有
5. 其他已有用户设置

不需要导出运行时状态，例如：

```text
当前是否限制中
最近拦截记录
临时测试状态
```

## 文件格式

建议使用 JSON。

示例文件名：

```text
NightGuard_Config_2026-05-28.json
```

## 实现要求

1. 新增配置导入导出服务，例如 `ConfigImportExportService`。
2. 导出时生成一个 JSON 文件。
3. 导入时读取 JSON 文件并覆盖或合并当前配置。
4. 导入前最好弹窗确认。
5. 导入失败要有明确错误提示。
6. 导出成功要显示文件路径。
7. 导入后刷新页面显示。
8. JSON 结构要带版本号，方便以后升级。

## 推荐 JSON 结构

```json
{
  "version": 1,
  "exportedAt": "2026-05-28T23:10:00",
  "appRules": [],
  "hostRules": [],
  "scheduleRules": [],
  "settings": {}
}
```

## Codex 执行 Prompt

请在现有 NightGuard 项目中新增“设置导入/导出”功能。

要求：

- 使用 JSON 格式导出配置。
- JSON 顶层包含 version 和 exportedAt。
- 至少导出应用拦截规则、hosts 规则、时间限制配置、已有用户设置。
- 不导出最近拦截记录、当前是否限制中、临时测试状态等运行时数据。
- 在设置页面增加“导出配置”和“导入配置”按钮。
- 导入前弹窗确认，避免误覆盖。
- 导入失败要显示明确错误原因。
- 导入成功后刷新页面数据。
- 尽量复用现有配置读写逻辑，不要重复维护两套配置。
- 完成后说明修改文件、测试步骤、风险点。

## 验收标准

1. 点击导出后能生成 JSON 文件。
2. JSON 文件能看到规则和设置。
3. 删除或修改当前配置后，可以通过导入恢复。
4. 导入错误 JSON 时不会崩溃，有错误提示。
5. 导入后 UI 自动刷新。

---

# 阶段 4：启动时自检

## 目标

启动软件后显示当前运行环境状态，减少使用时的迷惑。

## 自检项

首页或设置页显示以下状态：

```text
管理员权限：是/否
hosts 可写：是/否
自启动任务：已启用/未启用
当前是否限制中：是/否
```

## 实现要求

1. 新增自检模型，例如 `StartupCheckResult`。
2. 新增自检服务，例如 `StartupCheckService`。
3. 软件启动时执行一次自检。
4. 首页显示自检结果。
5. 提供“重新检测”按钮。
6. 检测失败不能影响软件启动。
7. 对异常情况给出简短建议。

## 检测逻辑建议

### 管理员权限

判断当前进程是否以管理员身份运行。

### hosts 可写

检测 hosts 文件是否可写。  
注意：不要真的破坏 hosts 内容。可以尝试以写权限打开文件，或者使用安全方式检测权限。

### 自启动任务

根据项目现有自启动实现判断：

- 如果使用任务计划程序，就检查对应任务是否存在并启用。
- 如果使用注册表，就检查对应注册表项。

### 当前是否限制中

读取现有限制状态，不要另起状态。

## Codex 执行 Prompt

请在现有 NightGuard 项目中新增“启动时自检”功能。

要求：

- 新增自检服务，检测管理员权限、hosts 可写、自启动任务状态、当前是否限制中。
- 软件启动后自动检测一次。
- 首页显示检测结果。
- 提供“重新检测”按钮。
- hosts 可写检测必须安全，不能修改或清空 hosts 内容。
- 检测异常不能导致程序崩溃。
- 对异常项显示简短建议，例如“请以管理员身份运行”。
- 尽量复用现有自启动和限制状态代码。
- 完成后说明修改文件、测试步骤、风险点。

## 验收标准

1. 普通权限启动时，管理员权限显示“否”。
2. 管理员权限启动时，管理员权限显示“是”。
3. hosts 不可写时显示“否”并有提示。
4. 自启动状态能正确显示。
5. 限制中状态能正确显示。
6. 点击重新检测后状态会刷新。

---

# 阶段 5：更安全的测试模式

## 目标

当前测试模式如果会进入真实限制逻辑，风险较高。  
需要改成“只测试指定测试程序”，默认只影响 `NightGuardTestApp.exe`，不碰其它真实应用和 hosts。

## 新测试模式原则

1. 测试模式不进入完整真实限制逻辑。
2. 测试模式默认不修改真实 hosts。
3. 测试模式只拦截指定测试程序。
4. 默认测试进程名：

```text
NightGuardTestApp.exe
```

5. 如果测试程序不存在，只显示“未检测到测试程序”，不要误伤其它程序。

## 可选增强

可以提供一个“生成测试程序”或“打开测试说明”的按钮。  
但第一版不强制。

## 实现要求

1. 查找当前测试模式入口。
2. 将测试模式和真实限制逻辑解耦。
3. 新增测试配置，例如 `TestModeOptions`。
4. 测试模式只检查并拦截 `NightGuardTestApp.exe`。
5. 测试结果写入最近拦截记录。
6. UI 明确提示：

```text
安全测试模式只会尝试拦截 NightGuardTestApp.exe，不会修改 hosts，不会影响其它应用。
```

## Codex 执行 Prompt

请重构 NightGuard 当前测试模式，让它变成更安全的测试模式。

要求：

- 测试模式不能进入完整真实限制逻辑。
- 测试模式默认不修改真实 hosts。
- 测试模式只检测并拦截指定测试进程：NightGuardTestApp.exe。
- 如果没有检测到该进程，只显示未检测到测试程序，不要处理其它进程。
- 测试结果写入最近拦截记录或日志。
- UI 文案明确说明测试模式不会影响其它应用和 hosts。
- 不要破坏真实限制模式。
- 完成后说明修改文件、测试步骤、风险点。

## 验收标准

1. 点击测试模式，不会写入 hosts。
2. 点击测试模式，不会关闭 Steam、浏览器、微信等真实应用。
3. 只有运行 NightGuardTestApp.exe 时，测试模式才会尝试拦截它。
4. 测试结果能显示在最近拦截记录中。
5. 真实限制模式仍然正常工作。

---

# 阶段 6：发布成单文件 exe

## 目标

把 NightGuard 发布成一个更适合自用的单文件 `NightGuard.exe`，减少开发运行、路径混乱、dotnet 启动混乱的问题。

## 实现要求

1. 检查项目框架版本和运行时。
2. 使用 `dotnet publish` 发布 Windows 单文件 exe。
3. 输出到固定目录，例如：

```text
publish/NightGuard
```

4. 尽量生成单文件。
5. 如果项目需要管理员权限，补充说明如何以管理员身份运行。
6. 如果使用 appsettings、配置文件、资源文件，要确认发布后路径仍正常。
7. 不要删除开发环境配置。

## 推荐命令

如果是 .NET 6/7/8 桌面项目，可参考：

```powershell
dotnet publish -c Release -r win-x64 `
  -p:PublishSingleFile=true `
  -p:SelfContained=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -o publish/NightGuard
```

如果不想打包运行时，可用：

```powershell
dotnet publish -c Release -r win-x64 `
  -p:PublishSingleFile=true `
  -p:SelfContained=false `
  -o publish/NightGuard
```

## Codex 执行 Prompt

请帮我把 NightGuard 发布成 Windows 单文件 exe。

要求：

- 先识别项目使用的 .NET 版本、项目类型、入口 csproj。
- 给出适合当前项目的 dotnet publish 命令。
- 优先生成 win-x64 Release 单文件 exe。
- 输出目录使用 publish/NightGuard。
- 检查发布后的 exe 是否能启动。
- 检查配置文件、资源文件、hosts 权限、自启动相关逻辑是否受路径影响。
- 如果单文件发布不适合当前项目，请说明原因，并给出最稳替代发布方案。
- 不要删除源码或开发配置。

## 验收标准

1. 能生成 `NightGuard.exe`。
2. 双击能启动。
3. 以管理员身份运行后，hosts 和进程拦截功能正常。
4. 配置读写路径正常。
5. 退出重启后配置仍保留。

---

# 最终整体验收清单

全部阶段完成后，请按以下清单检查：

```text
[ ] 首页有最近拦截记录，显示最近 20 条
[ ] 限制开始/结束会产生记录
[ ] hosts 添加/移除会产生记录
[ ] 应用拦截会产生记录
[ ] 娱乐模板可以一键添加
[ ] 娱乐模板重复添加不会产生重复规则
[ ] 配置可以导出 JSON
[ ] 配置可以从 JSON 导入
[ ] 启动时能显示管理员权限状态
[ ] 启动时能显示 hosts 可写状态
[ ] 启动时能显示自启动状态
[ ] 启动时能显示当前是否限制中
[ ] 测试模式不会影响真实应用
[ ] 测试模式不会修改真实 hosts
[ ] 测试模式只处理 NightGuardTestApp.exe
[ ] 可以发布成 NightGuard.exe
[ ] 发布版配置读写正常
```

---

# 给 Codex 的最终总结 Prompt

当所有阶段完成后，把下面这段发给 Codex，让它做最终复查。

```text
请对 NightGuard 本轮功能迭代做最终复查。

重点检查：

1. 最近拦截记录是否覆盖进程拦截、hosts 修改、限制开始/结束、测试模式。
2. 娱乐规则模板是否去重，是否不会破坏手动规则。
3. 配置导入/导出是否不会导出运行时状态。
4. 启动自检是否安全，尤其 hosts 可写检测是否不会修改 hosts 内容。
5. 测试模式是否已经和真实限制逻辑解耦，是否只处理 NightGuardTestApp.exe。
6. 发布成 exe 后，配置路径、资源路径、自启动路径是否仍正常。
7. 是否存在可能误伤真实应用或误改 hosts 的风险。

请输出：

- 本轮修改摘要
- 已完成项
- 未完成或存疑项
- 需要我手动测试的步骤
- 发布版使用注意事项
```
